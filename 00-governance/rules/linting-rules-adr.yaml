
# ==============================================================================
# ⚠️ WARNING: AUTOMATED DOCUMENTATION LINKAGE ⚠️
# ==============================================================================
# This YAML file is the Single Source of Truth (SSOT). If you modify any rules
# below, you MUST regenerate the Markdown table in the respective GDC document.
# 
# Run this command from the scnehaux-architecture root directory:
# > python scripts/generate_rules_doc.py
# ==============================================================================
config:
  target_doc: GDC-011-adr-guideline.md

rules:
  metadata:
    required_fields:
    - id
    - title
    - adr_type
    - status
    - created
    - created_by
    allowed_statuses:
    - proposed
    - accepted
    - rejected
    - superseded
    - deprecated
    exception_info_required_fields:
    - approved_by
    - expiry_date
    - risk_classification
    - exception_reason
    allowed_types:
    - foundational
    - implementation
    - exception
    - conflict_resolution
    - replacement
  structure:
    required_sections:
    - Title
    - Status
    - Context
    - Decision Drivers
    - Decision
    - Consequences
    - Compliance Impact
    - Alternatives Considered
