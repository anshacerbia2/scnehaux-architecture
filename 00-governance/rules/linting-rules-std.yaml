
# ==============================================================================
# ⚠️ WARNING: AUTOMATED DOCUMENTATION LINKAGE ⚠️
# ==============================================================================
# This YAML file is the Single Source of Truth (SSOT). If you modify any rules
# below, you MUST regenerate the Markdown table in the respective GDC document.
# 
# Run this command from the scnehaux-architecture root directory:
# > python scripts/generate_rules_doc.py
# ==============================================================================
config:
  target_doc: GDC-008-std-guideline.md

rules:
  metadata:
    required_fields:
    - id
    - title
    - owner
    - version
    - status
    - classification
    allowed_statuses:
    - adopted
    - trial
    - assessed
    - hold
    allowed_classifications:
    - public
    - internal
    - restricted
  structure:
    required_sections:
    - Objective & Scope
    - Design Principles
    - Normative Rules
    - Exceptions
    - Enforcement Mechanism
  content:
    prohibited_section_keywords:
      Exceptions:
      - waiver
      - ADR
      - ARB
      - Approval Requirements
